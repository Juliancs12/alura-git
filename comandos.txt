para inicializar un proyecto empezamos con el comando el
**git init
esto lo usamos en la ubicacion de la carpeta donde queramos 
en crear el repositorio


con **git status se sabe el estao en el que se encuentra el proyecto en
basicamente, con este se puede reconocer  el estado en el
en el que se empieza el proyecto y los archivos que estan siendo monitoreados y cuales no

con **git add . se adicionan todos los archivos que se desean ratrear
o **git add <nombre del archivo> del archivo solo q1  a 1 que se desea guardar

para quitar el archivo de git podemos usar el archivo
**git rm --cached <file>

para abrir la configuracion inicial de git es necesario registrar un correo
estose hace así 
**git config --global user.email "<Email>"
**git config --global user.name "<usuario>"

se agregan los cambios de los archivos que fueron afectados anteriormente con la
**git add . o  ** git add <file>
y luego se comitean 

git commit -m "acento arreglado en curso de integración continua"